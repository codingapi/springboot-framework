version: "3"

services:
  mobile-nginx:
    image: nginx:latest
    volumes:
      - "./mobile:/usr/share/nginx/html"
    environment:
      TZ: "Asia/Shanghai"
    restart: always
    ports:
      - "13000:80"